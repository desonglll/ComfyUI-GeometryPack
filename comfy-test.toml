[test]
name = "ComfyUI-GeometryPack"
python_version = "3.11"
cpu_only = true
timeout = 300
levels = ["syntax", "install", "registration", "instantiation", "validation"]

[test.workflows]
files = [
    "workflows/mesh_info.json",
    "workflows/transform.json",
    "workflows/boolean_ops.json",
]
timeout = 120

[test.platforms]
linux = true
windows = true
windows_portable = true
