[test]
levels = ["syntax", "install", "registration", "instantiation", "static_capture", "execution"]

[test.workflows]
timeout = 300
cpu = "all"
gpu = "all"

[test.platforms]
linux = true
macos = true
windows = true
windows_portable = true
